# 개발일지 2026-05-21: Phase 3 구현 + 상담일지 복사 모달

> **작성일:** 2026-05-21  
> **브랜치:** develop  
> **대상:** AI 추천 채용정보 개선 (Phase 3, Task 4) + 상담일지 복사 서브모달 + 스케줄러 OOM 대응

---

## 1. 채용공고 스케줄러 OOM 대응 + 대시보드 KPI 가중치

### 1.1 채용공고 스케줄러 OOM 대응 — 페이지 단위 동기화

**문제:** 기존 `fetchAllForSync()` 방식은 고용24 API의 전체 공고(최대 60,000건)를 `List<RecruitmentPostingDTO>`에 한번에 누적하여 OOM 발생 위험이 있었다.

**해결:** 페이지 단위(100건)로 API 호출 → 즉시 DB UPSERT → GC 가능하도록 아키텍처 변경.

| 파일 | 변경내용 |
|------|----------|
| `RecruitmentServiceImpl.java` | `syncPostingsByPage(syncDtm)` 메서드 추가. `RestTemplate` 타임아웃(10s/30s) 설정. `MAX_DETAIL_PER_SYNC` 60000→500 |
| `RecruitmentService.java` | `syncPostingsByPage(String syncDtm)` 인터페이스 추가 |
| `RecruitmentSyncResultDTO.java` | **신규** — 동기화 결과 추적 DTO |
| `RecruitmentScheduler.java` | 기존 `fetchAllForSync()` 대신 `syncPostingsByPage()` 호출 |
| `RecruitmentServiceImplPageSyncTest.java` | **신규** — 페이지 단위 동기화 단위 테스트 |

### 1.2 대시보드 KPI 가중치 반영

| 파일 | 변경내용 |
|------|----------|
| `Dashboard-mapping.xml` | KPI 산출 쿼리에 `A.가중치` 컬럼 반영. 1년미만 인원 포함/미포함 주석 정정 |
| `DashboardMainController.java` | TODO 주석 정리 |

### 1.3 기타

| 파일 | 변경내용 |
|------|----------|
| `pom.xml` | 버전 1.5.0 → 1.6.05 |
| `testClass.java` | `@TestPropertySource` 추가, 테스트 구직번호 변경 |
| `AI_Function_1Week_Progress.md` / `프로젝트-현황.md` | JS 버전 참조 업데이트 |

**커밋:** `b6ee2e0` — `[feat][develop]: 채용공고 스케줄러 OOM 대응 페이지 단위 동기화 + 대시보드 KPI 가중치 반영`

---

## 2. Phase 3: AI 추천 채용정보 개선 (Task 4)

### 2.1 Task 4-2: 삭제 정책 변경 — soft delete + 6개월 hard delete

**기존:** `deleteOldPostings(syncDtm)` — API에서 사라진 공고를 즉시 하드 삭제  
**변경:** 비활성화(soft delete) → 6개월 후 하드 삭제 2단계 처리

| 파일 | 변경내용 |
|------|----------|
| `Recruitment-mapping.xml` | `deactivateOldPostings` 신규 (is_active=0, deactivated_at=GETDATE()). `deleteOldPostings` 조건 변경 (is_active=0 AND deactivated_at < 6개월). MERGE UPDATE에 `is_active=1, deactivated_at=NULL` 복구 추가. `selectNewPostingIds`에 `AND is_active=1` 추가 |
| `RecruitmentDAO.java` | `deactivateOldPostings(String)` 추가, `deleteOldPostings()` 파라미터 제거 |
| `RecruitmentScheduler.java` | 호출 순서: 페이지 동기화 → 비활성화 → 6개월 삭제 → 상세수집 |

**배포 DDL:**
```sql
ALTER TABLE J_참여자관리_JOB_POSTING ADD deactivated_at DATETIME NULL;
```

### 2.2 Task 4-3: AI 추천 후보 is_active 필터 추가

| 파일 | 변경내용 |
|------|----------|
| `ParticipantJobRecommend-mapping.xml` | `selectJobInfoCandidates` 쿼리에 `AND is_active = 1` 조건 추가 |

### 2.3 Task 4-1: 상담일지 복사 기능

**초기 구현:** `copyJobInfo()` — 단순 `navigator.clipboard.writeText()` + `Swal.fire()`  
**테스트 결과:** SweetAlert2가 추천 모달(z-index: 9999) 뒤에 가려짐. 사용자가 체크박스 기반 항목 선택 서브모달을 요구.

**재설계 구현 (상담일지 복사 서브모달):**

| 파일 | 변경내용 |
|------|----------|
| `JobPostingCopyDTO.java` | **신규** — JOB_POSTING 29개 컬럼 매핑 DTO |
| `sql-map-config.xml` | `jobPostingCopy` typeAlias 등록 |
| `ParticipantJobRecommend-mapping.xml` | `selectJobPostingDetail` 쿼리 추가 (wanted_auth_no 기준 단건 조회) |
| `ParticipantJobRecommendDAO.java` | `selectJobPostingDetail(String)` 메서드 추가 |
| `ParticipantJobRecommendService.java` + `Impl` | `getJobPostingDetail(String)` 메서드 추가 |
| `recommendAjaxController.java` | `POST /api/recommend/jobPostingDetail` 엔드포인트 추가 |
| `recommendModal.jsp` | 상담일지 복사 서브모달 HTML (z-index: 10001, 체크박스 27개 + 미리보기 textarea) |
| `recommend-modal_0.0.6.js` | `openCopyLogModal`, `closeCopyLogModal`, `updateCopyPreview`, `executeCopyLog`, `toggleMoreCopyColumns`. ESC 키 핸들러 업데이트. `bindRecommendList`에 `data-cert-no` 추가 |
| `recommend-modal_0.0.4.css` | 서브모달 스타일 + `.swal-over-modal { z-index: 99999 }` |
| `participantMain.jsp` | CSS `0.0.4`, JS `0.0.5` 참조 |

**동작 흐름:**
1. 추천 목록에서 "상담일지" 버튼 클릭
2. AJAX `POST /api/recommend/jobPostingDetail` (wanted_auth_no 기준)
3. 서브모달 표시 (z-index: 10001, 추천 모달 위)
4. 기본 11개 항목 체크됨 → 미리보기 자동 갱신
5. "더보기"로 추가 16개 항목 선택 가능
6. "복사하기" → 클립보드 복사 + SweetAlert2 알림

**기본 항목:** 기업명, 채용공고, 업종, 직무, 지원자격(우대조건+자격면허+전공), 급여, 고용형태, 근무지역, 추천사유, 공고URL, 정보제공처(고용24 고정)

**더보기 항목:** 대표자명, 회사규모, 근로자수, 자본금, 연매출, 사업내용, 홈페이지, 채용기간, 모집인원, 직무내용, 근무시간, 4대보험, 퇴직금, 복리후생, 경력, 학력

### 2.4 Task 4-4: Kakao 공유 시 마감 공고 알림

| 파일 | 변경내용 |
|------|----------|
| `ParticipantJobRecommendDTO.java` | `isActive` (Boolean) 필드 추가 |
| `ParticipantJobRecommend-mapping.xml` | `selectRecommendList` — `JOB_POSTING` LEFT JOIN 추가, `ISNULL(P.is_active, 0) AS isActive` (삭제된 공고 = 비활성) |
| `recommend-modal_0.0.6.js` | `bindRecommendList`에 `data-is-active` 추가. `shareJobViaKakao()`에 마감 확인 SweetAlert2 경고. `inactive-row` CSS 클래스 |

---

## 3. 불필요 파일 정리

| 삭제 파일 | 사유 |
|-----------|------|
| `recommend-modal_0.0.2.css` | CSS `0.0.4`로 대체됨 |
| `recommend-modal_0.0.3.css` | CSS `0.0.4`로 대체됨 |
| `recommend-modal_0.0.3.js` | JS `0.0.5`로 대체됨 |
| `recommend-modal_0.0.4.js` | JS `0.0.5`로 대체됨 |

---

## 4. 버그 수정 이력

| 버그 | 원인 | 수정 |
|------|------|------|
| SweetAlert2 모달 뒤에 숨김 | 추천 모달 z-index(9999) > Swal 기본값 | `.swal-over-modal { z-index: 99999 }` CSS + 모든 `Swal.fire()`에 `customClass` 적용 |
| Kakao 공유 삭제된 공고 경고 없음 | `ISNULL(P.is_active, 1)` — 삭제된 공고도 활성 처리 | `ISNULL(P.is_active, 0)`으로 변경 (삭제 = 비활성) |
| 더보기 TypeError null | JSP에 id 없이 class만 사용 + JS `getElementById` | `querySelector('#copyLogModal .copy-column-more')`로 변경 |
| 정보제공처 DB 값 출력 | DB 값이 null이거나 다른 값일 수 있음 | `infoSvc` 키는 항상 `'고용24'` 고정 출력 |

---

## 5. Phase 4: 비밀번호 확인 페이지 보안 수정 (Task 6)

### 5.1 보안 취약점 수정

**문제:** 비밀번호가 없는 사용자(DB에 NULL/빈문자열)가 마이페이지에 무인증 접근 가능

| 파일 | 변경내용 |
|------|----------|
| `AsyncMyPage.java` | `passwordEmpty` 시 인증통과 → `PASSWORD_REQUIRED` 상태 반환으로 변경. `loginSelect` 쿼리(비밀번호 포함)로 먼저 조회하여 비밀번호 유무 판단 |
| `myPageJS_0.0.3.js` (신규) | `PASSWORD_REQUIRED` 응답 처리 → 비밀번호 설정 화면 표시. `#setupPasswordBtn` 클릭 핸들러 (유효성 검증 + `/changeMyPassword.api` 호출) |
| `myPage.jsp` | JS 참조 `0.0.2`→`0.0.3`. `password-setup-section` HTML 삽입 |

### 5.2 버그 수정: 비밀번호 틀려도 설정 페이지로 이동

**원인:** `OneMemberDataSelectNotPasswordCheck` 쿼리가 비밀번호 컬럼을 SELECT하지 않아 `storedPW`가 항상 `null` → 모든 사용자가 `PASSWORD_REQUIRED`로 처리됨

**수정:** `loginSelect` 쿼리(비밀번호 포함)로 먼저 조회 → 비밀번호 유무/검증 → 인증 성공 후 `OneMemberDataSelectNotPasswordCheck`로 마이페이지 데이터 조회

### 5.3 불필요 파일 정리

| 삭제 파일 | 사유 |
|-----------|------|
| `myPageJS_0.0.1.js` | JS `0.0.3`으로 대체됨 |
| `myPageJS_0.0.2.js` | JS `0.0.3`으로 대체됨 |

---

## 6. 빌드 검증

- `./mvnw clean compile` — 성공

---

## 7. 향후 계획

- Phase 2 (Task 2 + Task 5) — 참여자 조회 페이지 개편 + 검색 필터 개편 (UI 변경 범위 넓어 운영팀 사전 협의 후 진행)
- ~~Phase 4 (Task 6) — 비밀번호 확인 페이지 보안 수정~~ **완료**
- Task 1 (집단상담/단기특강) — 요건 구체화 후 별도 개발일지로 작성
- `deactivated_at` DDL 배포 — 운영 서버에 ALTER TABLE 실행 필요