<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!--
    ============================================================================
    ⚠️  이 web.xml 파일은 Java Config 마이그레이션으로 인해 대부분 사용되지 않습니다.
    ============================================================================

    Java Config로 전환된 항목:
    - Context 로딩: WebAppInitializer.java에서 처리
    - DispatcherServlet: WebAppInitializer.java에서 처리
    - Session 설정: WebAppInitializer.java에서 처리

    web.xml에 남아있는 이유:
    - Error Page 설정은 web.xml에서만 가능 (또는 @ControllerAdvice 사용)

    참고:
    - applicationContext.xml → RootConfig.java
    - ds-servlet.xml → WebMvcConfig.java, WebSocketConfig.java
    - web.xml → WebAppInitializer.java (일부)

    마이그레이션 날짜: 2025-11-11
    ============================================================================
    -->

    <!-- Error Page 설정 (유지) -->
    <error-page>
        <error-code>404</error-code>
        <location>/error/error404.jsp</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/error/error500.jsp</location>
    </error-page>

    <!--
    ============================================================================
    아래 설정들은 WebAppInitializer.java로 이동되었습니다.
    참고용으로 주석 처리하여 보관합니다.
    ============================================================================

    <session-config>
        <session-timeout>360</session-timeout>
    </session-config>

    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>classpath:applicationContext.xml</param-value>
    </context-param>

    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <servlet>
        <servlet-name>ds</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>ds</servlet-name>
        <url-pattern>*.do</url-pattern>
        <url-pattern>*.login</url-pattern>
        <url-pattern>*.api</url-pattern>
        <url-pattern>/jobPlacement/*</url-pattern>
    </servlet-mapping>
    -->

</web-app>