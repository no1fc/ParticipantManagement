package com.jobmoa.app.config;

/**
 * 보안 관련 설정 클래스.
 *
 * <p>현재는 빈 클래스이며, 향후 Spring Security 필터 체인 등
 * 보안 정책을 정의할 때 사용한다.
 * 비밀번호 인코더({@code BCryptPasswordEncoder})는 {@link RootConfig}에서 빈으로 등록되어 있다.
 */
public class SecurityConfig {
}
